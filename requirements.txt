# This requirements file lists all third-party dependencies for this project.
#
# Run 'pip install -r requirements.txt -t pylib/' to install these dependencies
# in `pylib/` subdirectory.
#
# Note: The `pylib` directory is added to `sys.path` by `appengine_config.py`.
Flask==0.10
XlsxWriter==0.6.6
